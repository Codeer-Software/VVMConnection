<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:l="clr-namespace:WpfApp"
        xmlns:j="clr-namespace:VVMConnection;assembly=VVMConnection"
        Title="MainWindow" Height="350" Width="525">
    <Window.DataContext>
        <l:MainWindowVM/>
    </Window.DataContext>

    <j:Connection.Methods>
        <j:MethodCollection>
            <j:Method Name ="ShowText" Invoker="NotifyText"/>
            <j:Method Name ="Show" Invoker="Ask">
                <j:Method.Target>
                    <l:MessageBox Caption="タイトル" Button="{x:Static MessageBoxButton.YesNo}"/>
                </j:Method.Target>
            </j:Method>
        </j:MethodCollection>
    </j:Connection.Methods>

    <StackPanel>
        <Button Content="HelloWorld-Event" Click="{j:Event HelloWorld }"/>
        <Button Content="HelloWorld-Command" Command="{j:Command HelloWorld }"/>
        <StackPanel Orientation="Horizontal">
            <TextBox Width="100" Text="{Binding Number.Value, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="×2" Command="{j:Command Calculate, CanCalculate }"/>
            <TextBlock Text="{Binding Answer.Value}" Margin="10,0,0,0"/>
        </StackPanel>
    </StackPanel>
</Window>
